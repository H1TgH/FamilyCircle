services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ".env"
    depends_on:
      - postgres

  postgres:
    image: postgres:latest
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - "postgres.env"

volumes:
  postgres_data:
